{{- if .installUv -}}
#!/bin/bash
set -euo pipefail

if ! command -v uv &> /dev/null; then
    echo "Installing uv..."
    curl -fsSL https://astral.sh/uv/install.sh | sh
else
    echo "uv is already installed."
fi
{{- end }}
