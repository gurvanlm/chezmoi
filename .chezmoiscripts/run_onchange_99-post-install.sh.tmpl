{{- if .installCliTools -}}
#!/bin/bash
set -euo pipefail

# Rebuild bat theme cache
# hash: {{ include "dot_config/bat/themes/tokyonight_night.tmTheme" | sha256sum }}
if command -v bat &> /dev/null; then
    echo "Rebuilding bat cache..."
    bat cache --build
fi
{{- end }}
